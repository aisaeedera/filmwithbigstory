# Big Story — Client Handoff & TODO

The site is built and deploys as-is (text-only, Phase 1). Everything below is a
follow-up that needs an asset or a credential from the client. Each item is
marked in code with a `TODO(client)` comment.

## 1. Assets to supply (Phase 2)

| Asset | Where it plugs in | Spec |
|---|---|---|
| **Showreel video** | Home `/` showreel strip + service pages | Cloudflare Stream ID. Swap the placeholder block in `src/app/[locale]/page.tsx`. 90–120s, 16:9. |
| **Case-study stills** | `/work/[slug]` hero + `/work` cards | 16:9, 2400px, AVIF/WebP, ~250KB. |
| **Founder / team headshots** | `/about` team strip | 1:1, 800px, monochrome/warm-toned. Replace placeholder tiles + `about.team.note`. |
| **Client logos** | Home trust strip | SVG or transparent PNG. Replace the service-tile grid in `page.tsx`. NDA-cleared only. |
| **Real OG image (optional)** | social preview | Currently auto-generated by `src/app/[locale]/opengraph-image.tsx`. Replace with a designed 1200×630 if preferred. |

## 2. Credentials to set in Vercel env

| Env var | Purpose |
|---|---|
| `RESEND_API_KEY` | Sends the contact-form brief as email. Until set, leads are logged server-side only (nothing is sent). |
| `LEAD_INBOX` | Destination inbox for briefs (defaults to `hi@filmwithbigstory.com`). |

Optionally add **Vercel Analytics** or **Plausible** for privacy-friendly analytics.

## 3. Content to verify with Saeed

- **Studio address** — only "Dubai, UAE" is shown publicly. Add full address if wanted (schema + contact page).
- **Arabic copy** — full bilingual copy is written and live, but should get a native-speaker QA pass before heavy Arabic-SEO push.
- **Case studies** — the 3 launch entries are concept frameworks (matching the old site). Replace with real client work as projects wrap.
- **Team names** — add real founder/crew names + `/team/[name]` pages in Phase 2.

## 4. DNS / migration

- Move `filmwithbigstory.com` DNS to Big Story's Cloudflare, point at Vercel.
- 301 redirects for legacy Layer B URLs (`/collections/*`, `/products/*`, `/bundles/*`,
  `/camera-rental-*` → `red.filmwithbigstory.com`) are already configured in `next.config.ts`.
- Submit `/sitemap.xml` to Google Search Console after cutover.

## 5. What's already done

- 10 core EN pages + 7 service pages + 42 location pages + 3 case studies — mirrored in Arabic (`/ar/*`). ~130 routes total.
- WhatsApp-first CTAs with pre-filled brief, secondary email, tertiary form.
- Schema.org: Organization, LocalBusiness/ProfessionalService, WebSite, Service (areaServed), FAQPage, BreadcrumbList.
- `robots.txt` (AI crawlers allowed), `sitemap.xml` (hreflang + priority + changefreq), `llms.txt`, `llms-full.txt`.
- Dark + gold cinematic design system, Fraunces display serif + Montserrat + Tajawal/Aref Ruqaa Arabic.
- WCAG: skip link, focus states, reduced-motion, semantic HTML, RTL.
